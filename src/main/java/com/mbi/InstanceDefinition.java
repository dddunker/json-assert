package com.mbi;

class InstanceDefinition {

    void assertEntity(Object a, Object e) {
//        Assertion asserter;
//        if (a instanceof JSONArray && e instanceof JSONArray) {
//            asserter = new Assertion((JSONArray) e, (JSONArray) a);
//        } else if (a instanceof JSONObject && e instanceof JSONObject) {
//            asserter = new Assertion((JSONObject) e, (JSONObject) a);
//        } else if (a instanceof JSONArray && e instanceof JSONObject[]) {
//            asserter = new Assertion((JSONObject[]) e, (JSONArray) a);
//        } else if (a instanceof Response && e instanceof JSONArray) {
//            asserter = new Assertion((JSONArray) e, new JSONArray(a.toString()));
//        } else if (a instanceof Response && e instanceof JSONObject) {
//            asserter = new Assertion((JSONObject) e, new JSONObject(a.toString()));
//        } else if (a instanceof Response && e instanceof JSONObject[]) {
//            asserter = new Assertion((JSONObject[]) e, new JSONArray(a.toString()));
//        } else {
//            throw new Error("Error arguments passed");
//        }
    }
}
